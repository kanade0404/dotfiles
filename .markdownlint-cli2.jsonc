{
  // markdownlint-cli2 config — CodeRabbit CI が使う markdownlint-cli2 と揃える。
  "config": {
    "default": true,
    // 日本語の散文 + コードブロックで行長制限は実用的でないため無効化
    "MD013": false,
    // 同一階層でのみ見出し重複を禁止 (手順書で "注意" 等が再出するため)
    "MD024": { "siblings_only": true },
    // 末尾の絵文字 prefix コミット例などで許容したい場合に備え句読点は緩める
    "MD026": false,
    // テーブルの pipe 周りスペースは整形の好みであり正しさではない (repo 全体で
    // compact/loose 混在。CI の本質は MD040 等の構造的エラー検出)
    "MD060": false
  },
  "globs": ["**/*.md"],
  "ignores": [
    "node_modules",
    // 外部から取り込んだ skill バンドル (本リポジトリで著者管理していない)
    ".rulesync/skills/.curated/**",
    // claude 用隔離パイプラインの cache (codex と同じく lint 対象外)
    "rulesync-claude/.rulesync/**",
    ".agents/skills/**",
    // .claude/skills/ も .agents/skills/ と同様 rulesync 生成物 (source は別 repo / upstream)。
    // 著者管理する source 側で扱うべきもので、生成コピーは lint 対象外にする。
    ".claude/skills/**",
    ".claude/plans/**",
    ".claude/plugins/**",
    ".claude-plugin/**",
    "**/CHANGELOG.md"
  ]
}
